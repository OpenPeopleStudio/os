import { NormalizedEvent } from './calendar.types';

export const filterByPersona = (events: NormalizedEvent[], persona: string): NormalizedEvent[] => {
  // TODO: persona-aware filtering
  return events;
};

export const filterByMode = (events: NormalizedEvent[], mode: string): NormalizedEvent[] => {
  // TODO: mode-aware filtering
  return events;
};
